name: 'Action AI'
description: 'Process actions from GitHub issues using a reducer system'
inputs:
  github-token:
    description: 'GitHub token'
    required: true
    default: ${{ github.token }}
runs:
  using: 'node20'
  main: 'dist/index.js'
branding:
  icon: 'message-circle'
  color: 'blue'